# ExMachina Workspace

如果你是 OpenClaw 并直接打开了这个仓库，请先读取 `BOOTSTRAP.md`。

核心要求：

- 不要把这个仓库当成普通代码仓库直接开始执行任务。
- 先读取 `openclaw-pack/install/INSTALL.md` 和 `openclaw-pack/install/openclaw.agents.plan.json`。
- 在没有完成安装计划前，默认由主控体 `exmachina-main` 接收任务。

